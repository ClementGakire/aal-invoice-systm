# Database environment variables - you'll need to add your actual Vercel Postgres connection string
# Get these from: https://vercel.com/dashboard -> Your Project -> Storage -> Create Database
# After creating a Postgres database, copy the connection string here:

# Example format (replace with your actual connection string):
# POSTGRES_URL="postgresql://username:password@hostname:port/database?sslmode=require"
# POSTGRES_PRISMA_URL="postgresql://username:password@hostname:port/database?sslmode=require&pgbouncer=true&connect_timeout=15"
# POSTGRES_URL_NO_SSL="postgresql://username:password@hostname:port/database"
# POSTGRES_URL_NON_POOLING="postgresql://username:password@hostname:port/database?sslmode=require"
# POSTGRES_USER="username"
# POSTGRES_HOST="hostname"
# POSTGRES_PASSWORD="password"
# POSTGRES_DATABASE="database"

# For now, you can create a database at https://vercel.com/dashboard
# Then copy the environment variables and paste them here